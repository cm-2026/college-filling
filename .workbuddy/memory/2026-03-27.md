# 2026-03-27 工作记录

## 明确选科两字段分工：必选→subject_type，再选→subject_require（18:22）

**需求**：明确区分匹配规则 —— 必选科目（物理/历史）匹配 `subject_type`，再选科目（化学/生物等）匹配 `subject_require`，`subject_require = '不限'` 时只要必选符合即可

**数据库字段确认**（admission_plan 表）：
- `subject_type`：只有 `"物理"` 和 `"历史"` 两种值（精确匹配 `=` 即可）
- `subject_require`：10种值，均为再选科目组合（化学、生物、化学和生物等）和"不限"
- `"不限"` 记录数：40405条（约占总数2/3）

**三级优先级查询（UNION ALL）**：
- P1（priority=1）：`subject_type = ?` + `subject_require IN ('化学和生物','生物和化学')` → 2门再选完全匹配
- P2（priority=2）：`subject_type = ?` + `subject_require IN ('化学','生物')` → 匹配1门再选
- P3（priority=3）：`subject_type = ?` + `subject_require = '不限'` → 必选匹配，再选无限制

**修改**：
1. `backend/server.js`：重写注释，明确两字段分工；更新 console.log 输出，显示必选/再选科目信息
2. `backend/server.js`：P2/P3 的 matchNote 文案从"(仅化学)"改为"(仅需化学)"、从"(不限选科)"改为"(不限再选)"

## 推荐院校卡片整体可点击，新窗口打开详情（18:14）

**需求**：点击推荐结果中的院校卡片，在新窗口打开学校详情页

**修改**：
1. `index-mysql.html`：`.recommendation-card` 加 `cursor: pointer` + `onclick="window.open('school-detail.html?schoolName=...', '_blank')"` 整卡跳转
2. `index-mysql.html`：院校名的 `<a target="_blank">` 改为普通 `<span>`，避免双重跳转
3. `index-mysql.html`：启用原被注释的 `.recommendation-card:hover` 样式，加 `translateY(-2px)` 上浮效果

## 删除所有选填筛选框（18:09）

**需求**：去掉表单中全部选填筛选项，保留核心三项（生源地、分数、选科组合）

**删除的字段**：目标地区偏好(targetRegion)、招生类别(majorPreference)、学校性质(graduationPlan)、报考批次(batchFilter)、学科大类(categoryFilter)、专业类(majorCategoryFilter)、性格类型(personality)

**修改**：
1. `index-mysql.html`：删除7个选填字段的全部 HTML（三行 div.row）
2. `index-mysql.html`：getFormData() 精简为只返回 region/score/subjectCombination
3. `index-mysql.html`：fetch 请求体精简为只传 score/subjectCombination/region

## 新增学科大类和专业类筛选（17:58）

**需求**：根据 admission_plan 表中的 `category`（学科大类/专业大类）和 `major_category`（专业类）字段，增加两个新筛选条件

**数据库字段值**：
- `category`：35个枚举值，包含工学/理学/医学/管理学等本科门类，以及交通运输大类/电子与信息大类等职教大类
- `major_category`：190个枚举值，如计算机类、临床医学类、法学类等专业类

**修改**：
1. `index-mysql.html`：在报考批次下方新增一行，左侧"学科大类（选填）"下拉框（id=categoryFilter，35个选项按理工农医/人文社科/职业教育大类/其他分组），右侧"专业类（选填）"文本框（id=majorCategoryFilter，带 datalist 自动补全 + 模糊匹配提示）
2. `index-mysql.html`：getFormData() 添加 categoryFilter 和 majorCategoryFilter 字段
3. `index-mysql.html`：fetch 请求体加入 categoryFilter 和 majorCategoryFilter 参数，同时补上漏传的 majorPreference
4. `backend/server.js`：解构 req.body 添加 categoryFilter 和 majorCategoryFilter
5. `backend/server.js`：新增 categoryCondition（`AND category = ?` 精确匹配）和 majorCategoryCondition（`AND major_category LIKE ?` 模糊匹配）
6. `backend/server.js`：三个 UNION 子查询均加入两个新条件，queryParams 末尾追加 categoryParams 和 majorCategoryParams

**设计选择**：category 用精确匹配（枚举值固定），major_category 用模糊匹配（支持用户输入部分关键词）

## 修复 targetProvinces is not defined 报错（17:51）

**根本原因**：将地区筛选变量从 `targetProvinces` 重命名为 `targetKeywords` 时，log 语句中漏改了一处，导致运行时报 ReferenceError，所有推荐请求均返回 500 错误

**修复**：`backend/server.js` 第317行 `targetProvinces` → `targetKeywords`



**需求**：推荐结果卡片中的分组编号显示数据库 major_group_code 字段

**修改**：
1. `backend/server.js`：三个 UNION 子查询 SELECT 列表加入 `major_group_code`
2. `backend/server.js`：数据映射对象加入 `major_group_code: row.major_group_code || ''`
3. `index-mysql.html`：🏷️ 图标后改为显示 `uni.major_group_code`，格式为"第101组"



**需求**：将"专业方向意向"下拉框改为匹配数据库 batch_remark 字段的"招生类别"筛选

**数据库 batch_remark 实际值（主要类）**：普通类、军事类、定向培养军士类、国家专项计划、师范类（含公费/优师4子类）、公安类、医学类、司法类、地方专项计划、定向就业、航海类、少数民族预科班、行业类、其他类（多子类）

**修改**：
1. `index-mysql.html`：下拉框标签从"专业方向意向"改为"招生类别"，用 `<optgroup>` 分5组展示主要类别（id 保持 majorPreference 不变）
2. `backend/server.js`：解构 req.body 添加 majorPreference
3. `backend/server.js`：新增 batchRemarkCondition（`AND batch_remark LIKE ?`）和 batchRemarkParams（LIKE 模糊匹配）
4. `backend/server.js`：三个 UNION 子查询均加入 batchRemarkCondition，queryParams 末尾追加 batchRemarkParams



**需求**：根据 admission_plan 表中 batch 字段，在推荐页面增加批次筛选条件

**数据库 batch 实际值**：本科批、本科提前批、专科批、专科提前批、提前批(专科)、飞行技术批

**修改**：
1. `index-mysql.html`：在学校性质和性格类型之间新增"报考批次（选填）"下拉框（id=batchFilter），列出6个批次选项
2. `index-mysql.html`：getFormData() 函数添加 batchFilter 字段
3. `index-mysql.html`：fetch 请求体添加 batchFilter 参数
4. `backend/server.js`：解构 req.body 添加 batchFilter
5. `backend/server.js`：新增 batchCondition（`AND batch = ?`）和 batchParams
6. `backend/server.js`：三个 UNION 子查询均加入 batchCondition，queryParams 末尾追加 batchParams



**需求**：目标地区偏好输入框同时匹配 college_province 和 college_city 字段

**修改**：
1. `backend/server.js`：重构 targetFilter，从 `IN (?)` 省份精确匹配改为 `(college_province LIKE ? OR college_city LIKE ?)` 模糊匹配，支持多个关键词（逗号分隔），多关键词间用 OR 连接
2. `backend/server.js`：queryParams 中 `...targetProvinces` 改为 `...targetParams`（每关键词2个参数）
3. `index-mysql.html`：输入框 placeholder 更新为"支持省份或城市，如：北京、上海、成都"

## 隐藏轮播图指示器圆点（17:20）

**需求**：去掉推荐结果轮播图下方的圆点指示器

**修改**：`index-mysql.html` 轮播指示器容器 `display` 强制设为 `none`

## 前端页面配色调整（09:28）

**需求**：根据《权鼎志愿》书籍封面配色，统一调整三个前端页面的颜色风格

**封面色系提取**：
- 深海军蓝背景：`#020e2e` / `#061540`
- 主蓝色：`#0d2566`
- 亮蓝强调：`#3b9fe8`
- 辅助中蓝：`#1e5bbf`
- 次要文字：`#a8ccf0`（冰蓝）
- 金色点缀：`#f0a500`（替换原来的pink/粉色）
- 浅冰蓝装饰：`#7dc8f0`

**改动的三个文件**：
- `index-mysql.html`：全量替换所有旧颜色（约30处）
- `college-list.html`：全量替换所有旧颜色
- `major-list.html`：CSS变量更新

**配色映射**：
| 旧颜色 | 新颜色 | 说明 |
|--------|--------|------|
| `#0f0c29 → #302b63 → #24243e` | `#020e2e → #0d2566 → #061540` | 背景渐变 |
| `rgba(20,20,40,0.6)` | `rgba(10,30,80,0.65)` | 卡片背景 |
| `rgba(100,100,255,0.15)` | `rgba(59,159,232,0.2)` | 卡片边框 |
| `#00d4ff` (accent-cyan) | `#3b9fe8` | 主强调色 |
| `#a855f7` (accent-purple) | `#1e5bbf` | 辅强调色 |
| `#f472b6` (accent-pink) | `#f0a500` | 点缀色 |
| `#34d399` (accent-green) | `#7dc8f0` | 冰蓝装饰 |
| `#b0b0d0` (text-secondary) | `#a8ccf0` | 次要文字 |

**状态**：✅ 已完成

## 地区选择改为模糊搜索Combobox（13:31）

- `index-mysql.html` 地区选择从 `<select>` 改为自定义 combobox
- 支持：文字输入 + 拼音首字母搜索（如输入 hn 匹配河南/湖南）
- 支持：↑↓键导航，Enter确认，Esc关闭，点击外部关闭
- 下拉列表按高考模式分3组：3+1+2（24省）、3+3（6省市）、传统文理（1区）
- 搜索时高亮匹配字符，无结果时显示"未找到匹配省份"
- 选择省份后自动触发选科模式切换逻辑（同原select行为）
- 表单提交增加region校验（hidden input无原生required支持）

**状态**：✅ 已完成

## 大明白高报数据导入（16:31）

**数据源**：`河南-23~22数据报24高考验证（25计划）-大明白高报数据表-家长练习版.xlsx`  
**目标表**：`gaokao.henan_admission_plan`

**表结构**（61字段）：
- 基础信息：id、year、source_province、batch、subject_type、college_code、college_name 等
- 专业信息：major_code、major_full_name、major_name、subject_require、school_years、tuition、category 等
- 2024录取：min_score_1、min_rank_1、avg_score_1（1后缀）
- 2023录取：同上，2后缀
- 2022录取：同上，3后缀
- 院校地理：college_province、college_city、city_level

**数据量**：60585条，2337所院校，历史类20235条，物理类40350条

**导入过程**：
- 第一次失败1500条（major_full_name VARCHAR(200)太短，扩充为VARCHAR(300)）
- 第二次全部成功：60585/60585条，失败0条

**相关文件**：
- `e:\xm\backend\create_henan_plan.sql`：建表SQL
- `e:\xm\backend\import_henan_plan.py`：Python导入脚本

**状态**：✅ 已完成

## admission_plan 表重新导入（16:55）

**数据源**：`河南数据.xlsx`（新版简化版，58列，无ID列和专业全称列）  
**操作**：TRUNCATE 清空 + 重新导入，60585条全部成功，失败0条  
**脚本**：`e:\xm\backend\import_henan_new.py`  
**处理差异**：新Excel无ID列（用行号1~60585生成），无专业全称（major_full_name用专业名称代替），无录取测试列（admit_verified置NULL）

**状态**：✅ 已完成

## 推荐接口改用admission_plan表（17:07）

**改动文件**：`backend/server.js` - `/api/recommend-from-db` 接口

**核心变化**：
- 从旧的 `henan`/`guangdong` 动态表名 → 统一查 `admission_plan` 表
- 生源地：前端 `region` → SQL `source_province`
- 分数匹配：`min_score` → `min_score_1`（2024年最低录取分）
- 选科匹配：数据库格式是"化学和生物"（"和"连接，不含必选科）

**3级优先级匹配**：P1完全匹配2选填科 > P2匹配单科 > P3不限选科  
**验证**：河南550分物理+化学+生物返回3429条，优先级排序正确

**状态**：✅ 已完成
