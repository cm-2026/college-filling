# 2026-03-28 工作记录

## admission_plan 表字段说明（15:21）

- 表中无 college_level、college_type 字段
- 院校层次信息来自 recommend_reason 字段（格式如"985/211/双一流"），在JS映射层解析为college_level
- 城市级别字段为 city_level（不是院校层次）
- 完整字段已确认，避免再次引入不存在字段

## 修复 college_level 字段名错误（15:20）

- 报错：Unknown column 'college_level' in 'field list'
- 原因：admission_plan表中实际字段名为 college_type，不是 college_level
- 修复：两处SQL查询改为 `college_type AS college_level`，别名保持不变，前端无需修改

## recommend-results.html 新增城市筛选联动（15:17）

- 新增城市筛选下拉（filterCity），放在省份筛选右侧
- 省份-城市联动：选择省份后城市下拉自动更新为该省可选城市
- provinceCity 全局变量存储省份→城市映射
- applyFilter 添加城市筛选条件（r.college_city匹配）
- resetFilter 同步清空城市下拉
- 筛选栏从5列变为每行5个，共10个筛选项两行显示

## recommend-results.html 省份筛选城市字段修正（15:15）

- 确认 admission_plan 表本身有 college_city 字段
- 移除之前错误添加的 JOIN dxmessage 联表查询
- 直接用 SELECT college_city FROM admission_plan，性能更优

## major-list.html 背景色修改（14:33）

- 将 `--bg-gradient` 从渐变色改为纯色 `#0F296C`

## major-list.html 数据源改为 admission_plan（14:25）

- 后端 `/api/majors`：统计数据（school_count/min_score/max_score/avg_score）从 `henan` 表改为 `admission_plan` 表，字段映射：`college_name→去重计数`，`min_score_1→min_score`
- 后端 `/api/major-detail`：录取院校数据从 `henan` 表改为 `admission_plan` 表，字段映射：`college_name/college_code/college_type/college_province/min_score_1/min_rank_1/plan_count_1/college_level/batch_remark`
- 前端 `major-list.html`：院校表格新增"院校层次"列和"批次类别"合并显示，表头从8列扩为10列

## recommend-results.html UI优化（13:39-13:42）

- tooltip宽度从320px加大到500px
- 默认每页显示10所院校（原50），下拉选项新增「10所/页」
- 筛选栏重构为CSS Grid布局（3列），标签+控件纵向对齐，所有控件统一height:34px，移除零散style
- 筛选底部新增filter-actions行，「筛选结果N条」左对齐，「重置筛选」按钮右侧
- 排序按钮统一height:32px + padding:0 16px，视觉整齐
- 分页按钮统一height:32px，与排序按钮一致

## 专业介绍tooltip数据接入确认（11:43）

**核实**：
- admission_plan有1492个不同专业名，major_info有666条，50977条可匹配
- 后端 `/api/major-info` 接口返回 introduction/career_path/courses 三字段
- 改为：先精确匹配，无结果时用 LIKE 双向模糊兜底（处理专业名带后缀情况）
- 前端tooltip JS通过 `data-major` 属性取专业名，调用接口，结果缓存在 majorCache

---

## 专业名称悬浮显示专业介绍（11:39）

**需求**：鼠标悬浮专业名称时显示浮动介绍卡片
**改动**：
1. `.major-name` 加 `cursor:pointer`、虚线下划线，悬浮变色
2. 新增 `#majorTooltip` 浮层 DOM（固定定位，320px宽，毛玻璃背景）
3. 新增 tooltip JS：事件委托 `mouseover`/`mousemove`/`mouseout`
4. 调用后端 `/api/major-info?major_name=xxx` 获取 `introduction`、`career_path`、`courses`
5. 结果缓存到 `majorCache` 对象，避免重复请求
6. 自动定位：超出右/下边界时翻转方向
7. `.major-name` span 加 `data-major` 属性存储精确专业名

---

## 专业列表按院校折叠（11:33）

**需求**：专业信息按院校改为折叠/展开，默认展开
**改动**：
1. `.school-header` 加 `cursor: pointer`，`onclick` 切换 `.collapsed` 类
2. 院校链接加 `event.stopPropagation()` 防止点链接时触发折叠
3. 头部右侧加 `▼` 箭头 `.collapse-arrow`，collapsed 时旋转 -90°
4. 专业列头+专业行整体包裹进 `.major-body`，collapsed 时 `max-height:0; opacity:0` 过渡动画收起
5. 关闭 div 改为 `</div></div>`（major-body + school-card）

---

## 专业行信息合并一行显示（11:31）

**需求**：专业名称、组号、专业类在同一行横向显示
**改动**：
1. `.major-row-left` 由 `flex-direction: column` 改为 `flex-direction: row`，`align-items: center`，`gap: 6px`
2. `.major-meta` 改为 `display: contents`，使组号和专业类标签融入父级 flex 行
3. 最终效果：`现代物流管理` `第101组` `物流类` 一行排列

---

## 院校信息合并一行显示（11:29）

**需求**：院校名称、性质、省份、批次、招生类别等信息全部在同一行显示
**改动**：
1. `recommend-results.html` 将 `.school-name-row` 和 `.school-tags` 合并：把省份/批次/batch_remark 直接放入 `.school-name-row` 的 flex 容器中
2. `.school-tags` 改为 `display:contents`（保留节点但不产生盒模型），元素融入父级 flex 行
3. 删除 `margin-bottom:6px` 使单行更紧凑

---

## 推荐结果页改为卡片式布局（11:22）

**需求**：参考院校志愿网截图，优化院校+专业的显示布局
**改动**：
1. 删除旧 table/thead/tbody 结构，改为 `.school-card` 卡片列表
2. 院校卡片 = 头部（院校名+层次标签+省份+批次+专业数）+ 专业列表列头 + 专业行
3. 专业行使用 CSS Grid（6列：专业名/最低分/最低位次/选科/招生计划/录取概率）
4. 排序改为筛选栏上方的 `.sort-bar` 行内按钮（最低分默认▼降序）
5. 专业名下方显示组号标签+专业类小字
6. 院校头部显示批次和招生类别（batch_remark）双标签
7. 分页按院校数，默认 50所/页

## 推荐结果筛选栏加重置按钮（11:15）

**改动**：筛选栏末尾加"重置"按钮，点击调 `resetFilter()`：清空所有输入框/下拉，重置二级联动（招生类别/专业类），用全量数据重建学科大类选项，最后触发 `applyFilter()`。样式与筛选栏一致（浅蓝描边）。

## 推荐结果表格改为院校分组显示（10:47）

**需求**：同一院校的多条专业记录合并在一起显示
**方案**：rowspan 分组渲染 + 分页按院校数计算
- `renderTable` 重写：用 Map 按 school_code+name 分组，每组第一行 `<td rowspan=N>` 显示院校信息（名称、省份、批次、院校层次标签），后续行只显示专业列
- 表头去掉独立的省份/批次列（9列→7列），省份+批次+院校层次挪入院校 cell 内联显示
- 分页改为按院校数分页，pageInfo 显示"X所院校/Y条专业"
- 新增院校层次彩色标签样式（211=蓝、985=金、双一流=紫、其他=绿）
- 组间分隔线：`tr.school-group-start td` 加顶部蓝色边框 + 左侧 accent 竖线
- 所有 colspan 从9改为7

## 推荐结果页增加院校层次筛选（10:41）

**需求**：根据 recommend_reason 第一个/前的值（211/双一流/普通公办等）增加院校层次筛选
**数据格式**：recommend_reason 如 "211/一线城市/专业有硕士点"，取 split('/')[0] 即为院校层次
**改动**：
1. `backend/server.js`：SELECT 加 `recommend_reason`，return 对象增加 `college_level: recommend_reason.split('/')[0]`
2. `recommend-results.html`：筛选栏在省份后加"院校层次"下拉（`filterCollegeLevel`）
3. `initFilterOptions` 填充院校层次选项（全量数据一次性填充，不需动态联动）
4. `applyFilter` 第一阶段 preFiltered 加 `collegeLevel` 过滤（院校属性，参与动态刷新学科大类）

## 学科大类/专业类选项根据已筛结果动态加载（10:37）

**需求**：学科大类下拉的选项，只显示当前已筛选院校中实际存在的大类
**方案**：两阶段筛选 + `refreshCategoryOptions(dataSet)` 动态重建
1. `applyFilter` 第一步：按院校/省份/批次/选科等条件筛出 `preFiltered` 中间结果
2. 用 `preFiltered` 调 `refreshCategoryOptions`，重建学科大类/专业类选项（保留当前选中值，如果新选项里已无该值则自动清空）
3. 第二步：再按 category/majorCategory 对 preFiltered 二次过滤得到 filteredData
4. `initFilterOptions` 初始化时改为调用 `refreshCategoryOptions(allData)`，不再手动建 categories 列表

## 推荐结果页增加学科大类/专业类二级联动筛选（10:30）

**需求**：筛选栏根据 category（学科大类）和 major_category（专业类）增加二级联动
**改动**：
1. `backend/server.js`：SELECT 加 `category`/`major_category`，return 对象补字段
2. `recommend-results.html`：筛选栏加"学科大类"(`filterCategory`) + "专业类"(`filterMajorCategory`)
3. `majorCategoryMap` 映射 + `onCategoryChange()` 联动函数（与批次联动模式一致）
4. `applyFilter()` 加 category/majorCategory 过滤条件
**重启后端生效**

## 批次筛选升级为两级联动菜单（10:20）

**需求**：推荐结果页批次筛选改为两级：一级 batch（本科一批等）+ 二级 batch_remark（招生类别）
**改动**：
1. `backend/server.js`：SELECT 中加 `batch_remark` 字段，return 对象中加 `batch` / `batch_remark`
2. `recommend-results.html`：筛选栏新增"招生类别"下拉（`filterBatchRemark`）
3. 新增 `batchRemarkMap` 映射表 + `onBatchChange()` 函数：选一级批次后动态更新二级选项
4. `applyFilter()` 加入 `batchRemark` 条件
**重启后端生效**

## 修复推荐结果 sessionStorage 超额问题（10:08）

**问题**：点击"获取推荐"报错 `Failed to execute 'setItem' on 'Storage': Setting the value of 'recommendResults' exceeded the quota.`
**原因**：推荐数据量大（数万条），超出 sessionStorage 5MB 限制
**解决方案**：改用 URL 参数传递查询条件，结果页自己调后端接口
- `index-mysql.html`：window.open 时带 URLSearchParams（region/score/subjectCombination）
- `recommend-results.html`：DOMContentLoaded 时从 location.search 读取参数，fetch POST `/api/recommend-from-db`，结果直接渲染；加载中/错误状态均有提示

## 推荐结果在新窗口独立页展示（09:53）

**需求**：点击"获取推荐"后，在新窗口打开完整结果页（而非在主页内滚动展示）

**方案**：sessionStorage 跨页传数据 + window.open 新标签

**新建文件** `e:\xm\recommend-results.html`：
- 从 `sessionStorage.recommendResults` 读取推荐数组、从 `sessionStorage.recommendMeta` 读取查询条件
- 顶部信息栏：生源地、分数、选科组合、院校数/专业数
- 筛选栏：院校名（模糊）、专业名（模糊）、省份（下拉）、批次（下拉）、选科要求（下拉）
- 平铺表格展示：院校/省份/批次/专业/组号/最低分/位次/选科要求/录取概率
- 支持表头点击排序（升/降切换）、分页（50/100/200/全部条/页可选）
- 院校名点击跳转 `school-detail.html`

**修改** `index-mysql.html`：
- 原 `displayResults(...)` 调用改为：存 sessionStorage → `window.open('recommend-results.html','_blank')` → showToast 提示
- 0条时显示警告 toast 不跳转



**需求**：按严格的筛选顺序重写查询：地区→必选科目→再选科目→分数（改用group_min_score_1）

**新查询逻辑（单一 SELECT，不再用 UNION ALL）**：
1. `source_province = ?`（生源地）
2. `subject_type = ?`（必选科目：物理/历史）
3. 再选科目 OR 条件：
   - `subject_require IN ('化学和生物','生物和化学')`（2门都含，两种排列）
   - `subject_require IN ('化学','生物')`（1门单科，用户再选中包含）
   - `subject_require = '不限'`（不限再选）
4. `group_min_score_1 <= ?`（组最低录取分 ≤ 用户分数）
5. 排序：`group_min_score_1 DESC, min_score_1 DESC`

**关键字段澄清**：
- 用户说的 `group_min_count1` = 实际字段 `group_min_score_1`（INT，组最低录取分）
- 旧逻辑用 `min_score_1 BETWEEN ±20` 已废弃，改为 `group_min_score_1 <= score`

**前端结果转换更新**：
- scoreDiff 基于 `group_min_score_1`（非 min_score_1）
- 去掉 match_priority，改用 subject_require 值判断 matchNote
- 返回 `score: groupScore`（组最低分），同时保留 `min_score: min_score_1`（专业最低分）
